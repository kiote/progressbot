language: python
python:
  - "3.5"
services:
  - postgresql
before_script:
  - psql -c 'create database progressbot_test;' -U postgres

env:
  global:
    - BUILD_ON_TRAVIS=true

install: "pip install -r requirements.txt"
before_script: python chat/models/creator.py
script: python test_server.py
